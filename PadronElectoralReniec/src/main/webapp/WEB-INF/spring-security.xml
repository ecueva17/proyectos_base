<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                                http://www.springframework.org/schema/beans/spring-beans.xsd
                                http://www.springframework.org/schema/security
                                http://www.springframework.org/schema/security/spring-security.xsd">

    <!-- DEBE IR PRIMERO -->
    <http pattern="/resources/**" security="none"/>

    <http auto-config="true"  use-expressions="true" disable-url-rewriting="true" >
        <intercept-url pattern="/resources/**" access="permitAll" />
        <intercept-url pattern="/login" access="permitAll" />
        <intercept-url pattern="/logout" access="permitAll" />
        <intercept-url pattern="/accessdenied" access="permitAll" />
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" />

        <!-- 2. AGREGAR AQUÍ: Desactiva CSRF para descartar bloqueos de seguridad -->
        <!--
        <csrf disabled="true"/>
        -->
    <form-login login-page="/login" default-target-url="/inicio" authentication-failure-url="/accessdenied" authentication-details-source-ref="myAuthDetailsSource"
    />

    <session-management invalid-session-url="/login" >
    </session-management>
    <logout logout-success-url="/login" />
    <headers />
</http>
<beans:bean id="myAuthDetailsSource" class="pe.gob.reniec.padronelectoral.filter.MyAuthenticationDetailsSource" />

<!-- custom-filter position="FORM_LOGIN_FILTER" ref="myFilter" /-->
    
    <!--beans:bean id="myFilter" class="pe.gob.reniec.padronelectoral.filter.ExUsernamePasswordAuthenticationFilter"/-->

    <authentication-manager>
        <authentication-provider ref="usuarioService">
            <!--<password-encoder ref="org.springframework.security.authentication.encoding.md5passwordencoder" />-->
        </authentication-provider>
    </authentication-manager>
    

</beans:beans>
